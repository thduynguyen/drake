Import('env')

env.Append(CPPFLAGS=['-DNO_THREADS'])
env.Append(CPPFLAGS=['-DNO_PTHREADS'])
env.Append(CPPFLAGS=['-DNO_NETWORK'])

common_x11 = [
    "os_dummy.cpp",
]

env.Prepend(LIBS=['glfw'])
godotvis_binary = env.Program('#bin/godotvis', ['godotvis.cpp'] + common_x11)
